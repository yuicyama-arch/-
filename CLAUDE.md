# SCRAP RAID(仮題) — 開発仕様書

見下ろし型ツインスティック PvE 脱出シューター。
「ブロスタの操作感 × CODの撃ち味 × デルタフォースの脱出構造」をスマホブラウザで。

---

## 0. Claude Code への指示(最初に読むこと)

- **マイルストーン(§11)の順に実装する。** 各マイルストーン完了時に一旦停止し、実機テスト手順と確認ポイントを提示して、開発者のフィードバックを待つこと。
- バランスに関わる数値は必ず `src/config/balance.ts` に一元化し、コード内に直書きしない。
- 撃ち味(§5)に関わる実装は最優先。入力遅延・ヒット演出で妥協しない。
- 中級スマホのブラウザで 60fps を維持できない実装は再設計する。
- 仕様に迷ったら「3分で1周できるサクサク感」を判断基準にする。

---

## 1. コンセプト

- 1ラン約3分。マップで物資(スクラップ)を漁り、敵AIを倒し、時間内に脱出する。
- **軽ロスト設計**: 死亡・時間切れ → そのランで拾った物資のみ没収。持ち込み武器と強化状態は失わない。脱出成功 → 物資を倉庫に加算。
- 気軽に何度も回して、倉庫で武器を強化し、少しずつ深部を攻略するメタ進行。

## 2. 技術スタック

- Phaser 3 + TypeScript + Vite
- 効果音は Web Audio で合成(後で音源ファイルに差し替え可能な構造にする)
- グラフィックは v1 は図形+単色スプライトのプレースホルダー(後で画像差し替え可能に)
- セーブは localStorage(セーブデータにバージョン番号を付与)

## 3. 画面と操作(横持ち固定)

- 縦持ち時は「横にしてね」オーバーレイを表示
- **左仮想スティック**: 移動
- **右仮想スティック**: ドラッグで照準(照準線を表示)、押している間フルオート射撃。タップのみ → 最寄りの敵へオートエイム射撃(ブロスタ方式)
- 右下に**リロードボタン**、残弾表示
- **茂み**: 中に入ると敵AIから視認されない+自機が半透明になる
- シーン構成: Boot → Menu → Loadout → Raid → Result

## 4. コアループ(1ラン = 3:00)

1. **出撃前**: ロードアウト選択(メイン武器1つ)
2. **ラン中**: クレート漁り+敵撃破ドロップでスクラップ・弾薬・設計図のかけらを集める
3. **脱出**: マップ内の脱出ゾーン(2箇所からランダムで1箇所が有効)で **3秒間滞在**で成功。滞在開始と同時に増援ウェーブが1回湧く(ここが山場)
4. 残り30秒で警告音+画面縁が赤く点滅。時間切れは死亡と同じ扱い
5. **リザルト画面**: 獲得/ロストした物資、撃破数、タイムを表示 → ワンタップで再出撃

## 5. 撃ち味仕様(品質の核・最重要)

- **反動**: 連射を続けると拡散角が拡大、射撃をやめると素早く収束(武器ごとに数値定義)
- **ヒットフィードバック**: ヒットマーカー表示+ダメージ数字ポップ+ヒット音。キル時は専用音+アイコン+敵の吹き飛び演出
- **ヒットストップ**: キル時に約40msだけ時間停止
- **画面揺れ**: 発砲時に微小、被弾時に中、キル時に小
- マズルフラッシュ、薬莢排出、着弾パーティクル、壁への弾痕(数秒で消える)
- リロードは円形プログレス表示。リロード中は移動速度そのまま(サクサク重視)

## 6. 武器バランス初期値(balance.ts の初期値)

| 武器 | ダメージ | 連射/秒 | 装弾 | リロード | 射程px | 拡散(初期→最大) | 備考 |
|---|---|---|---|---|---|---|---|
| AR ライノ | 12 | 8 | 30 | 1.8s | 550 | 2°→8° | 初期装備・基準 |
| SMG ホーネット | 7 | 13 | 35 | 1.5s | 380 | 4°→12° | 装備中移動+10% |
| SG ブルドッグ | 5×8ペレット | 1.2 | 6 | 2.5s(1発ずつ) | 260 | 12° | 近距離破壊力 |
| DMR ヘロン | 45 | 2 | 10 | 2.0s | 800 | 0.5° | 発砲すると全敵に位置がバレる |

- プレイヤー: HP 100(非戦闘5秒で毎秒10自動回復)、移動速度 200px/s

## 7. 敵AI(v1 は 3種)

状態機械: 巡回 → 発見 → 交戦 → 見失い(茂み対応)

| 敵 | HP | 攻撃 | 挙動 |
|---|---|---|---|
| グラント | 40 | 8/発・射程400 | 距離を保って撃つ。3〜4体の小隊で配置 |
| ラッシャー | 60 | 近接25 | 発見すると速度280で突進 |
| マークスマン | 30 | 30/発・射程700 | 1.2秒レーザー予告線 → 発砲。高台・奥に配置 |

- 撃破ドロップ: スクラップ小+弾薬(確率で設計図のかけら)
- ラン回数(通算)に応じて敵の配置数と構成が段階的に強化(難易度曲線は balance.ts で定義)

## 8. マップ

- v1 は手作り1枚(タイルベース、約2500×2500px)。外周から中央に向かうほど敵が濃く、報酬も良い
- 要素: 壁(遮蔽・弾を止める)、茂み(ステルス)、木箱(スクラップ小・弾)、軍用箱(スクラップ大・設計図のかけら、敵密集地帯に配置)、脱出ゾーン×2
- ミニマップ(右上・小)に自機と有効な脱出ゾーンのみ表示

## 9. メタ進行とセーブ

- 通貨: スクラップ
- **武器強化**: 各武器 Lv1→5(スクラップ消費)。1Lvごとにダメージ+5%・拡散-5%
- **武器解放**: 設計図のかけら×5 で新武器アンロック
- 倉庫画面(Menu内): 所持スクラップ、武器一覧、強化・解放ボタン

## 10. 演出・音

- SE: 発砲(武器ごとに音色を変える)、ヒット、キル、リロード、クレート開封、脱出カウント、警告
- BGM: v1 はなし(脱出カウント中のみ心拍風のループ音)
- リザルト画面で獲得物資がポンポン加算される演出

## 11. 開発マイルストーン(この順で。各M完了時に停止して報告)

- **M1 操作の土台**: 横持ちレイアウト、左スティック移動、右スティック照準+照準線、壁との衝突、茂みの透明化。→ 完了条件: 実機で操作が指に吸い付く感覚か確認待ち
- **M2 撃ち味の初版**: AR実装(反動・リロード込み)、グラント小隊、§5のヒットフィードバック一式。→ 完了条件: 「撃ってて気持ちいいか」の確認待ち
- **M3 コアループ完成**: 3分タイマー、クレート、脱出ゾーン+増援、リザルト画面、軽ロスト処理、再出撃。→ ここで初めて「ゲーム」になる
- **M4 拡充**: 武器4種+ロードアウト画面、敵3種、オートエイムタップ射撃
- **M5 メタ進行**: 倉庫・強化・解放・セーブ
- **M6 ポリッシュ**: 難易度曲線調整、初回チュートリアルラン、SE仕上げ、パフォーマンス最適化

## 12. コード方針

- ゲームロジック(判定・状態)と描画・入力を分離する。将来リアルタイム対人に拡張する際、ロジックをサーバー権威化しやすくするため
- ただし v1 で過剰な抽象化はしない
- 全定数は `src/config/balance.ts`。マップ定義は JSON

## 13. デプロイ

- GitHub リポジトリ + GitHub Actions で main push 時に GitHub Pages へ自動デプロイ
- README に公開URLを記載(スマホからワンタップでテストできる状態を維持)

## 14. v1 でやらないこと

リアルタイム対人 / 3D / 課金・ガチャ / ログイン / 縦持ち対応 / 複数マップ / ペット・スキルガジェット(v2候補)
